import { api } from "@/lib/api";

const App = () => {
  console.log(api);
  return (
    <div className="w-full h-full bg-white text-gray-900 text-4xl flex justify-center items-center">
      App
    </div>
  );
};

export default App;
